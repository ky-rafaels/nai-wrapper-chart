apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nai-easy-install.fullname" . }}-test-connection"
  labels:
    {{- include "nai-easy-install.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nai-easy-install.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
