{{- if .Values.naiLabs.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: nai-labs-sa
  namespace: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/component: nai-labs
    {{- include "nai-core.labels" . | nindent 4 }}
{{- if .Values.imagePullSecret.name }}
imagePullSecrets:
  - name: {{ .Values.imagePullSecret.name }}
{{- end }}
{{- end }}
