apiVersion: v1
kind: Service
metadata:
  name: {{ include "nai-easy-install.fullname" . }}
  labels:
    {{- include "nai-easy-install.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nai-easy-install.selectorLabels" . | nindent 4 }}
